"""Скриптовый уровень (DESIGN.md, §7).

Фаза 5. Два уровня с разными правилами:

* ``api`` — объект ``game`` для консоли аналитика. Телеметрия отдаётся
  обычными списками и словарями, чтобы её можно было считать вручную.
  Песочницы здесь нет намеренно: это машина игрока и его код.
* ``sandbox`` — изоляция для автоматизации из ``~/.local/share/module/scripts``:
  отдельный процесс, ``resource.setrlimit`` на CPU и память, whitelist AST,
  таймаут на тик. Упавший или зависший скрипт отключается с записью в журнал и
  никогда не вешает симуляцию.
"""
